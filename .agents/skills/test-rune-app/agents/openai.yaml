interface:
  display_name: "RUNE App Testing"
  short_description: "Launch and retain isolated RUNE test environments"
  default_prompt: "Use $test-rune-app to launch an isolated RUNE environment and iteratively test it in the browser while preserving state."
